import { MainPageComponent } from '../app/pages';
import { Layout } from '../app/components';

const MainPage = () => {
   return (
      <Layout>
         <MainPageComponent />
      </Layout>
   );
};

export default MainPage;
